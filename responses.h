#ifndef RESPONSES_H
#define RESPONSES_H

#include <string>
#include <vector>
#include <unordered_map>

extern std::unordered_map<std::string, std::vector<std::string>> responseMap;

#endif // RESPONSES_H